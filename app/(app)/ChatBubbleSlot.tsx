// Placeholder until Session 29 wires in the real chatbot. It exists now so the
// (app) layout's bottom spacing (BottomNav clearance, safe area) is settled
// once, rather than shifting when the real bubble lands.
export function ChatBubbleSlot() {
  return null;
}
